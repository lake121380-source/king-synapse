# Phase 7.4.2 Selected Source Authoring Successor v2

Status: bounded successor after classified v1 pre-freeze draft failure

## Retained failure

The v1 authoring adapter completed input, Schema, identity, hash, reference,
content-boundary, Provider, and authority checks. Its in-memory draft failed the
two preregistered cross-case diversity checks:

- 452 exact normalized text duplicates;
- 2,792 cross-case word-five-gram Jaccard pairs at or above `0.85`.

No authoritative v1 dataset, fixture, manifest, outcome, audit, state,
readiness, or receipt was written. No Reference, Gold, Atomic overlay, arm
output, effect score, Provider call, Runtime change, or dataset opening
occurred. The v1 adapter and its single append-only failed-attempt event are
retained as immutable evidence.

## Failure classification

The failure is:

```text
duplicate_or_similarity_failure
```

It is not evidence about RQ1, RQ2, Atomic effectiveness, or runtime value. It is
a pre-freeze dataset-construction failure caused by repeated template language.

## Bounded v2 authority

v2 may only change the source-authoring generator and output namespace in order
to remove cross-case text duplication while keeping the research design fixed.

Allowed changes are:

- deterministic case-specific lexical anchors;
- deterministic sentence-order or connective variation;
- domain/variant-specific surface wording;
- removal of generic repeated phrases that do not carry scenario facts;
- v2 dataset and audit artifact names.

v2 may not change:

- the 168 selected case IDs or their order;
- strata, domains, scenario variants, or selection ranks;
- entity, event, evidence, query, or Memory slot IDs;
- the ten candidate Memory slots, MemoryKind assignments, or pool ordinals;
- the six-event, eight-evidence, or ten-Memory counts;
- the source-case JSON Schema or frozen authoring contract;
- scenario facts merely to favor an arm;
- exact-duplicate threshold `0`;
- unresolved `0.85` word-five-gram similarity-pair threshold `0`;
- any Gold, Atomic, arm, metric, or statistical rule;
- Provider, Runtime, Store, RecallEngine, or production-write authority.

The v1 in-memory draft is not frozen source data and cannot be used as effect
data, a comparison arm, a pilot, or a basis for selecting cases. v2 starts from
the same frozen blank authoring plan.

## v2 disposition

A passing successor freeze authorizes only:

```text
author_phase7_4_selected_source_cases_v2
```

v2 must pass every original authoring check with unchanged thresholds. Another
failed preflight cannot be silently retried; it requires a separately recorded
successor. Atomic segmentation, Reference, Gold, arm execution, effect scoring,
Runtime Integration, productization, and release remain unauthorized.

