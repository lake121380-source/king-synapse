# v0.9.27 Public Demo And CI

Date: 2026-07-02

## Summary

This release turns the cognitive-memory work into a more approachable public
project page:

- README now opens with a product-style explanation, badges, quick start,
  comparison table, and Star History chart.
- `docs/DEMO.md` provides a disposable CLI walkthrough with real sample output.
- GitHub Actions CI is configured for formatting and the `synapse-eval` test
  suite.
- The checked-in external comparison report already records King Synapse,
  Graphiti/Zep, and Mem0 as measured systems.

## Validation

Local validation before tagging:

```bash
cargo fmt --all -- --check
cargo test -p synapse-eval
git diff --check
```

Result:

- `synapse-eval`: 40 tests passed.
- No whitespace diff issues.
- No API keys or bearer tokens found in README, docs, scripts, crates, or
  workflow files.

## External Comparison Snapshot

The latest checked-in report is:

`crates/eval/reports/external-comparison-latest.json`

Current snapshot:

- King Synapse: measured, full cognitive fixture coverage.
- Graphiti/Zep: measured locally through the Kuzu graph path.
- Mem0: measured through Mem0 OSS + DeepSeek + local Qdrant.
- Letta: adapter present, not configured locally yet.

## Next

- Let GitHub Actions run on `main` and fix CI if the hosted Linux environment
  exposes a dependency difference not seen locally.
- Configure Letta with a disposable API project or local endpoint.
- Add an external-run manifest for package versions, backend modes, and model
  names without storing secrets.
- Prepare LongMemEval and DMR fetch/cache instructions.

